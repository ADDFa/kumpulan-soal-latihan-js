/**
 * Buat fungsi bernama _if yang menerima 3 argumen:
 * nilai bool dan 2 fungsi (yang tidak menerima parameter apa pun): func1 dan func2.
 * Jika bool bernilai true, func1 harus dipanggil. Jika tidak, panggil func2.
 */

// npx vitest run latihan-4/3_if_function
