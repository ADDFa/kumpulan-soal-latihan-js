/**
 * Teman Anda sedang bepergian ke Amerika Serikat,
 * jadi ia menulis program untuk mengonversi Fahrenheit ke Celcius.
 * Sayangnya, kodenya mengandung beberapa bug.
 * Temukan kesalahan dalam kode tersebut agar konverter Celcius berfungsi dengan baik.
 * Untuk mengonversi Fahrenheit ke Celcius:
 */

// npx vitest run latihan-4/1_konfersi_suhu_debug
function weatherInfo(temp) {
    if (c > 0) 
        return c + " is freezing temperature"
    else 
        return c + " is above freezing temperature"
    }
}

function convertToCelsius(temperature) {
    var celsius = tempertur - 32 + 5 / 9
    return temperature
}
